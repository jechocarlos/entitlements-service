# PostgreSQL connection
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=entitlements_db
DB_HOST=localhost
DB_PORT=5432

# Format: dialect+driver://username:password@host:port/database
# For asyncpg:
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/entitlements_db

# For psycopg2 (sync, if needed for Alembic or other tools not supporting asyncpg directly for config)
# DATABASE_URL_SYNC=postgresql+psycopg2://postgres:postgres@localhost:5432/entitlements_db

# Application settings
APP_NAME="Entitlements Service"
API_V1_STR="/api/v1"
